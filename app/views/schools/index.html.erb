<% @page_title = "Schools|List" %>
<h3>Listing schools</h3>

<table class="table table-striped" style="font-size:14px;">
  <tr>
    <th>SCHOOL NAME</th>
    <th>ADDRESS</th>
    <th>ACTION</th>
  </tr>

<% @schools.each do |school| %>
  <tr>
    <td><%= school.schoolname %></td>
    <td><%= school.address %></td>
    <td><%= link_to 'Show', {:action=>'show', :id=>school.id},  :class=>"btn-link"  %> |
        <%= link_to 'Edit', {:action=>'edit', :id=>school.id},  :class=>"btn-link"  %> |
        <%= link_to 'Destroy', {:action=>'delete', :id=>school.id}, :class=>"btn-link"  %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New School', new_school_path , :class=>"btn btn-medium btn-info"%>
